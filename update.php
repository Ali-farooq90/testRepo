
<?php
/*$connup= mysqli_connect("localhost","Ali","1234","db1");
if ($connup) {
    echo "Connected to the database";
$uid = $_POST['uid'];
$uname = $_POST['uname'];
$upassword = $_POST['upassword'];
$usynced = "0";
    $up = "update users SET id= '$uid', name ='$uname', password='$upassword', synced ='$usynced' WHERE id = '$uid'";
$u= mysqli_query($connup,$up);
    if ($u){
        echo " Data Updated";
    }
    else {
        echo mysqli_error($connup);
    }
}

else{
    echo "Not connected to the database";
}
*/
?>